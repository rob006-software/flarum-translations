{
    "forumaker-bento": {
        "admin": {
            "settings": {
                "section_grid": "Tiles",
                "columns_desktop": "Number of tiles per row",
                "columns_desktop_help": "From 2 to 6 tiles per row on desktop. Mobile uses the standard Flarum navigation",
                "plain_create_button": "Remove fill from the «New Discussion» button",
                "plain_create_button_help": "Make it visually identical to the neighbouring tiles — no colored background"
            }
        }
    }
}
