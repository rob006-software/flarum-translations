{
    "import-ai-webhook": {
        "admin": {
            "settings": {
                "webhook_url_label": "Webhook URL",
                "webhook_url_help": "Enter the URL to receive POST requests when users create or edit posts. The payload includes event type, and full user, post, discussion, and actor model data.",
                "bearer_token_label": "Bearer Token",
                "bearer_token_help": "Optional Bearer token for Authorization header. The token will be sent as 'Authorization: Bearer <your-token>' with each webhook request."
            }
        }
    }
}
