# Galician inherited translations differences

Translations for Galician (`gl`) are inherited from Flarum 1.x, but they can be adjusted
independently after inheritance. This page lists all strings which have the same source string on both
sides, but do not match between them: **0** are translated differently and **1** are
translated only in `gl`. Altogether they cover **1** components.

<!-- {% raw %} -->


## Contents

| Component | Missing translations |
| --- | --- |
| `flarum-tags` | [1](#flarum-tags-missing) |


## Missing translations

These strings are translated only in `gl`, so there is nothing to inherit from Flarum 1.x - they could be used to fill the gaps there. Each entry contains the English source string, followed by the translation available only in `gl`.


### `flarum-tags` (missing)

#### [`flarum-tags.admin.edit_tag.title`](https://weblate.rob006.net/translate/flarum2/flarum-tags/gl/?q=context%3A%3D%22flarum-tags.admin.edit_tag.title%22)

> Create Tag

```diff
+=> flarum-tags.ref.create_tag
```

<!-- {% endraw %} -->
